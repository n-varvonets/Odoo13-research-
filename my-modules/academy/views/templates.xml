<odoo>
    <data>
        <template id="index">
            <t t-call="website.layout">
                <t t-set="title">Aca!!!demy</t>
                <div class="oe_structure">
                    <div class="container">
                        <t t-foreach="teachers" t-as="teacher">
                            <!--                            <p><t t-esc="teacher.id"/> <t t-esc="teacher.name"/></p>-->
                            <!--                            change the list of model to link to our new controller-->
                            <p>
                                <a t-attf-href="/academy/{{ slug(teacher) }}">/academy/{{ slug(teacher) }}(это ссылка в
                                    которой указан - имя класса teacher)
                                    <t t-esc="teacher.name"/>
                                    teacher.name
                                </a>
                            </p>
                        </t>
                    </div>
                </div>
            </t>
        </template>
        <template id="biography">
            <t t-call="website.layout">
                <t t-set="title">Academy</t>
                <div class="oe_structure"/>
                <div class="oe_structure">
                    <div class="container">
                        <h3>
                            <t t-esc="person.name"/>
                        </h3>
                        <p>Last modified:
                            <i t-field="person.write_date" t-options='{"format": "long"}'/>
                        </p><!-- write_date выводит дату последнего обновления(унаследованно в контролах от http) и t-options='{"format": "long"}' - переводит его в апрлеь(делает читабельным)-->
                        <p>Last modified:
                            <i t-field="person.write_date" t-options='{"widget": "relative"}'/>
                        </p><!-- 2 hours ago-->
                        <div>
                            <t t-esc="person.biography"/>
                        </div>
                    </div>
                    <div class="container">
                        <h3>
                            <t t-esc="person.name"/>
                            was born in X
                        </h3>
                        <p>that's number of {(<t t-esc="person.name"/>)"person.name"} id in our site {(<t
                                t-esc="person.id"/>)person.id}
                        </p>
                    </div>
                </div>
                <div class="oe_structure"/>
            </t>
        </template>
        <!--
                <template id="listing">
                  <ul>
                    <li t-foreach="objects" t-as="object">
                      <a t-attf-href="#{ root }/objects/#{ object.id }">
                        <t t-esc="object.display_name"/>
                      </a>
                    </li>
                  </ul>
                </template>
                <template id="object">
                  <h1><t t-esc="object.display_name"/></h1>
                  <dl>
                    <t t-foreach="object._fields" t-as="field">
                      <dt><t t-esc="field"/></dt>
                      <dd><t t-esc="object[field]"/></dd>
                    </t>
                  </dl>
                </template>
        -->
    </data>
</odoo>