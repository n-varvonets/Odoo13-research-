<odoo>

    <record model="ir.module.category" id="module_category_openacademy"><!-- данный блок нужен для отображения в http://i.imgur.com/VwfoWy8.png-->
        <field name="name">OpenAcademy(base)</field>
        <field name="description">Something that I wanna description in this category</field>
        <field name="sequence">10</field>
    </record>

    <record id="group_openacademy_manager" model="res.groups"><!-- создаем групы юзеров, которым дается определенный доступ -->
        <field name="name">OpenAcademy / Manager(___)</field>
        <field name="category_id"
               /><!-- ref="openacademy.module_category_openacademy" -  мы должны указать к какой катерии данная группа относится-->
    </record>

<!--next block of rule have to: Add a record rule for the model Course and the group “OpenAcademy / Manager”, that restricts write and
unlink accesses to the responsible of a course. If a course has no responsible, all users of the group must be able to modify it. http://i.imgur.com/BQQwiF4.png -->
        <record id="only_responsible_can_modify" model="ir.rule">
            <field name="name">Only Responsible can modify Course</field>
            <field name="model_id" ref="model_openacademy_course"/>
            <field name="groups" eval="[(4, ref('openacademy.group_openacademy_manager'))]"/><!-- только совместим(применяем к этой группе)-->
            <field name="perm_read" eval="0"/>
            <field name="perm_write" eval="1"/><!-- здесь 1 - означает что на это поле наложиться ограничение domain_forc true-->
            <field name="perm_create" eval="0"/>
            <field name="perm_unlink" eval="1"/>
            <field name="domain_force">
                ['|', ('responsible_id','=',False),
                      ('responsible_id','=',user.id)]
            </field>
<!-- т.е. для пользователей группы group_openacademy_manager если отвутсвует ответсвенный за курс или ответетсвенный
 за курс - это мы, то мы можем делать всё хотим с данным курсом(CRUD), в ином случае(если есть ответсвенный за курсы и
 это не мы)  - выполняются условия ограничения(что бы другие юзеры не смогли удалить твою запись)-->
        </record>

</odoo>